package main

import (
	"crypto/rand"
	"fmt"
	"strings"
)

func main() {
    // // 定义一个字符串切片
    // strs := []string{"Hello", "World", "!"}

    // // 使用 strings.Join 将切片中的所有字符串连接成一个字符串
    // result := strings.Join(strs, ",")

    // // 打印结果
    // fmt.Println(result)
    rand.Read()
}